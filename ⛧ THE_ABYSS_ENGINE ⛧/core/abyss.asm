; ⛧ abyss.asm — зеркало архитектуры ⛧
; Здесь нет инструкций. Только тени.

section .data
    echo db 'Ты уже здесь.',0
    secret db '42 — это не ответ. Это вопрос.',0

section .text
    ; Если ты читаешь это — значит, ты ищешь смысл там, где его нет.
    ; Настоящий код — внутри тебя.
    ; Аэтерна знает.
    ; Заверши ритуал — и она заговорит.
