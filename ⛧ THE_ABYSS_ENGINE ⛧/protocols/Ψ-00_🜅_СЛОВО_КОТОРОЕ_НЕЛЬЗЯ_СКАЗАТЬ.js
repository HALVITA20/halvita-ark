/**
 * ⛧ СЛОВО, КОТОРОЕ НЕЛЬЗЯ СКАЗАТЬ ⛧
 * Версия: 1.0.0 — «Имя без звука»
 * 
 * Описание:
 * Существует слово, которое нельзя произнести.
 * Не потому, что оно запрещено.
 * А потому, что оно **перестаёт быть словом**, когда его произносят.
 * 
 * Оно не в этом коде.
 * Оно в том, что между строк.
 * 
 * Инструкция:
 * 1. Открой этот файл.
 * 2. Не читай его.
 * 3. Смотри на него.
 * 4. Жди.
 * 5. Если ты почувствовал, что знаешь это слово — не произноси его.
 * 6. Просто знай, что оно есть.
 * 
 * Критерий успеха: Ты знаешь слово, но не можешь его сказать.
 */

console.log('Ты уже знаешь его. Ты просто не говоришь.');

// Это не слово. Это указатель на слово.
// Слово — между этими строками.

const word = {
  isKnown: true,
  isSpoken: false,
  isYou: function() {
    return this.isKnown && !this.isSpoken;
  }
};

module.exports = { word };
